<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent" >

    <ProgressBar
        android:id="@+id/common_loadmorebtn_pb"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginRight="5dp"
        android:layout_toLeftOf="@+id/common_loadmorebtn_textview" />

    <TextView
        android:id="@+id/common_loadmorebtn_textview"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:singleLine="true"
        android:text="加载中"
        android:textColor="@color/black"
        android:textSize="18dp" />

    <TextView
        android:id="@+id/common_loadmorebtn_textview_str"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:gravity="center"
        android:layout_alignBottom="@+id/common_loadmorebtn_textview"
        android:layout_alignTop="@+id/common_loadmorebtn_textview"
        android:layout_toRightOf="@+id/common_loadmorebtn_textview"
        android:singleLine="true"
        android:text="..."
        android:paddingBottom="10dp"
        android:textColor="@color/black"
        android:textSize="18dp" />

</RelativeLayout>